{
  "name": "voter",
  "workspaces": [
    "packages/common",
    "packages/client",
    "packages/server"
  ],
  "scripts": {
    "clean": "tsc --build --clean",
    "build": "tsc --build",
    "watch": "tsc --build --watch",
    "start": ""
  },
  "dependencies": {
    "fast-json-patch": "^3.1.1"
  }
}
